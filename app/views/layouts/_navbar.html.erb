<nav class="navbar navbar-expand-lg bg-body-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="<%= root_path %>">Cronos</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-start" id="navbarSupportedContent">
        
            <ul class="navbar-nav me-0 mb-2 mb-lg-0">
                <li class="nav-item">
                    <%= link_to root_path, class: "nav-link" do %>
                        <i class="fa-solid fa-download"></i> Downloaded
                    <% end %>
                </li>
                <li class="nav-item">
                    <%= link_to screens_path, class: "nav-link" do %>
                        <i class="fa-solid fa-magnifying-glass"></i> Discover
                    <% end %>
                </li>
            </ul>
        </div>

        <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
            <ul class="navbar-nav me-0 mb-2 mb-lg-0">

                <li class="nav-item">
                    <%= link_to user_screens_path(user_id: current_user.id), class: "nav-link" do %>
                        <i class="fa-solid fa-desktop"></i> Displays
                    <% end %>
                </li>
                <li class="nav-item">
                    <%= link_to root_path, class: "nav-link" do %>
                        <i class="fa-solid fa-gear"></i> Settings
                    <% end %>
                </li>

                <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    <% if user_signed_in? %> 
                        <i class="fa-regular fa-user"></i> <%= current_user.username %>
                    <% else %>
                        User
                    <% end %>
                </a>
                <ul class="dropdown-menu dropdown-menu-lg-end">
                    <% if user_signed_in? %>
                    <li>
                        <%= link_to edit_user_registration_path, class: "btn btn-default btn-sm dropdown-item" do %>
                            <i class="fa-solid fa-wrench"></i> Account
                        <% end %>
                    </li>
                    
                    <li>
                        <%= button_to destroy_user_session_path, method: :delete, class: "btn btn-default btn-sm dropdown-item" do %>
                            <i class="fa-solid fa-right-from-bracket"></i> Sign out
                        <% end %>
                    </li> 
                                
                    <% else %>
                        <li><%= link_to 'Sign up', new_user_registration_path, class: "dropdown-item"  %></li>
                        <li><%= link_to 'Log in', new_user_session_path, class: "dropdown-item" %></li>
                    <% end %>
                    </ul>
                </li>

                
            </ul>

            
            

            
        </div>
    </div>
</nav>