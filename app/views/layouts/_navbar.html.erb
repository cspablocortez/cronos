<nav class="navbar bg-body-primary">
    <div class="container">
        <a href="<%= root_path %>" class="navbar-nav h1" style="text-decoration: none">
            Cronos
        </a>
    
       
            <button class="btn btn-outline-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                User
            </button>
            <ul class="dropdown-menu">
                <% if user_signed_in? %>
                    <li class="dropdown-item"><%= link_to 'Account', edit_user_registration_path, class: "btn btn-default btn-sm" %></li>
                    <li class="dropdown-item"><%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-default btn-sm" %></li> 
                             
                <% else %>
                    <li class="dropdown-item"><%= link_to 'Sign up', new_user_registration_path %></li>
                    <li class="dropdown-item"><%= link_to 'Sign in', new_user_session_path %></li>
                <% end %>
            </ul>
        
    </div>
</nav>